; Urgentix - Emergency Department Visualization System
;
; [DATABASE] host is the MySQL server the application connects to. It can also
; be changed at run time from the Settings dialog on the login screen.
;
; [ADMIN] is the bootstrap database account the application uses for
; administrative operations such as creating other users. The values below are
; the documented defaults used to set up a local evaluation instance.
; CHANGE THEM BEFORE ANY REAL DEPLOYMENT.

[DATABASE]
host = localhost

[ADMIN]
user = emergency_admin
password = Urgentix2026!
